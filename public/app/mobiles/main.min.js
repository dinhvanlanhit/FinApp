$(document).ready(function(){
    $.ajaxSetup({headers: { 'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content') }});
    var pathname = window.location.href;
    var elmParent =  $('.appBottomMenu > a[href="'+pathname+'"]');
    elmParent.addClass('active');
    // appBottomMenu
});



